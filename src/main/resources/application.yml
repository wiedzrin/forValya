spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/for-valya}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:admin}
  jpa:
    generate-ddl: true
    show-sql: false